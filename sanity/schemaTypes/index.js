import artwork from './artwork'
import category from './category'
import exhibition from './exhibition'
import post from './post'

export const schema = {
  types: [artwork, category, exhibition, post],
}